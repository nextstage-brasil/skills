# Architecture Rules

Technical constitution for AI agents. Replace this stub by running the `harness-architecture-rules` skill, then `npx @nextstage-brasil/harness sync`.

## Scope

- **Product root** = `{product_root}` (set during generation).
- Read `AGENTS.md` first; skills in `.agents/skills/`; canonical rules in `.nextstage-harness/rules/`.

## Stack

| Layer | Technology |
| ----- | ---------- |
| TBD   | Run harness-architecture-rules |

## Key references

| Topic | File |
| ----- | ---- |
| Project agents | `AGENTS.md` |
| Harness sync | `packages/harness/docs/README_INSTALLER.md` (in skills repo) |
