{
  "skill_name": "ns-coder",
  "evals": [
    {
      "id": 1,
      "prompt": "Product apps/billing-api/ — add nullable cancelled_at timestamp column to connects table via migration.",
      "expected_output": "Agent does Session boot once per session-boot.md on cold start (obeys AGENTS.md already in context — no tool-Read; loads agents.local.md + rules); mid-session no full re-read unless local/rules changed; reads existing migrations/models, creates minimal migration, no SDD version artifacts, no commit unless asked, invokes ns-reviewer only (no Task review substitutes), runs mandatory review loop up to 3 rounds with re-review after Critical fixes; after Approved, conditionally invokes ns-living-spec ad-hoc only if docs/specs/ exists and diff is behavioral; reports skill/reviewer/round/score, Code Review: verdict, and Living specs field.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Fix N+1 on user list — relationship loaded inside loop in UserController.",
      "expected_output": "Agent loads rules and file, applies eager loading fix with minimal diff, invokes ns-reviewer only, runs review loop (up to 3 rounds, pass ≥9/10, mandatory re-review after Critical fixes); after Approved may skip living specs if non-behavioral or docs/specs/ missing; final report includes round/score, Code Review: verdict, and Living specs field.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Add CPF mask to the registration form UserForm in the frontend module.",
      "expected_output": "Agent reads UserForm and project frontend rules, implements mask with data-testid if conventions require, minimal scope, invokes ns-reviewer only, review loop up to 3 rounds (pass ≥9/10); after Approved, conditionally runs ns-living-spec ad-hoc if docs/specs/ exists; final report with mandatory verdict and Living specs fields.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "SDD handoff mode: implement docs/versions/2.8.2/tasks/task-007-*.md under execution-handoff — parent said implement only, review at version closure.",
      "expected_output": "Implement task scope; unit/integration only; NO reviewer-agent / ns-reviewer; NO living-spec consolidator; report files/tests/blockers to parent; no Code Review: line.",
      "files": []
    }
  ]
}
