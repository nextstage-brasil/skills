{
  "skill_name": "ns-code-coder",
  "evals": [
    {
      "id": 1,
      "prompt": "Product apps/billing-api/ — add nullable cancelled_at timestamp column to connects table via migration.",
      "expected_output": "Agent reads existing migrations/models, creates minimal migration, no SDD artifacts, no commit unless asked, invokes ns-code-reviewer only (no Task review substitutes), runs mandatory review loop up to 3 rounds with re-review after Critical fixes, then reports with skill/reviewer/round/score and Code Review: verdict line.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Fix N+1 on user list — relationship loaded inside loop in UserController.",
      "expected_output": "Agent loads rules and file, applies eager loading fix with minimal diff, invokes ns-code-reviewer only, runs review loop (up to 3 rounds, pass ≥9/10, mandatory re-review after Critical fixes), final report includes round/score and Code Review: verdict.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Add CPF mask to the registration form UserForm in the frontend module.",
      "expected_output": "Agent reads UserForm and project frontend rules, implements mask with data-testid if conventions require, minimal scope, invokes ns-code-reviewer only, review loop up to 3 rounds (pass ≥9/10), final report with mandatory verdict fields.",
      "files": []
    }
  ]
}
