# Session boot (mandatory)

**Canonical content lives in [`harness-discovery.md`](./harness-discovery.md#session-boot-blocking)** — section **Session boot (blocking)**.

Consumer skills must point to `harness-discovery.md` only. Do not duplicate the boot sequence in skill bodies or frontmatter `description`.
