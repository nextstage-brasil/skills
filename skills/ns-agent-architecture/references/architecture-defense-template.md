# Architecture defense pack — file schema

**Audience:** stakeholder / contractor presentation seed (slides later).  
**Not** implementation ADR. Never merge into `docs/specs/agent-architecture.md`.

**ADR is source of truth.** This pack restates failure / compensation / event tables for presentation. Do not invent cells the ADR lacks.

Write **`docs/specs/agent-architecture-defense.md`** only after Step 9 **yes**. Same locked language (`SKILL.md`). English headings below = agent schema; **written file** translates headings/cells (pattern ids Sequential, Parallel, Supervisor, Hierarchical, Group Chat, Handoff, CAP / Saga stay English labels).

Fill from locked decisions. CAP / saga / event gaps: grill one turn each — **never invent**.

Standalone (no FS): full pack in chat; save path `docs/specs/agent-architecture-defense.md`.

```markdown
# Architecture defense pack

> Generated by ns-agent-architecture skill (optional defense pack)
> **Source ADR:** `docs/specs/agent-architecture.md`
> **Use:** stakeholder presentation raw material — not implementation handoff

## Changelog

| Version | Date | Summary |
| ------- | ---- | ------- |
| `{version_san}` or `adhoc-YYYY-MM-DD` | {ISO date} | {what defense content changed} |

## Decision defense

One row per locked choice a contractor may challenge.

| Decision | Options considered | Selection criterion | Chosen | Cost if wrong | Expected objection | Answer |
| -------- | ------------------ | ------------------- | ------ | ------------- | ------------------ | ------ |
| Framework | [A / B] | [control vs speed / …] | [chosen] | [business impact] | [one sentence] | [one sentence] |
| One vs many agents | [generalist / specialists] | [vocab / tools / risk] | [chosen] | [audit vs coordination] | […] | […] |
| Orchestration (per key segment) | [patterns] | [dependency type] | [chosen] | […] | […] | […] |
| HITL placement | […] | […] | […] | […] | […] | […] |
| Consistency vs availability (key node) | [block / proceed-with-gap] | […] | […] | […] | […] | […] |

Add MVP-cut / other challenged locks. **CAP / saga / event:** grill until locked — **FORBIDDEN** `assumed`. Other cells (objection / answer / cost): grill preferred; `assumed — validate with stakeholder` only if human deferred.

## Boundary canvas (one vs many)

**Central question:** one generalist covering all agent rows worse than specialists?

| Dimension | Same across agent rows? | Evidence from this product |
| --------- | ----------------------- | -------------------------- |
| Vocabulary / audience | yes / no | [concrete] |
| Tool set | yes / no | [concrete] |
| Regulatory risk / reflection depth | yes / no | [concrete] |

**Split rule:** diverge **2+** dimensions → specialists. Regulatory risk alone may suffice.

**Coordination cost:**
- One agent: cheap coordinate; hard audit (mixed reasoning)
- Many agents: costly coordinate (protocol + shared state); easier audit (error isolated)

**Locked conclusion:** [one generalist | specialists — list names] — [one-sentence why]

**Agent vs tool:** same input → same logic = tool, not agent. Classified rows stay SoT.

## Orchestration pattern selector

| Transition / segment | Pattern | Why (dependency signal) | Rejected alternative |
| -------------------- | ------- | ----------------------- | -------------------- |
| [A → B or segment] | Sequential \| Parallel \| Supervisor \| Hierarchical \| Group Chat \| Handoff | [data dependence / calendar / routing / …] | [one clause] |

Copy ADR **Orchestration pattern**; expand every segment stakeholders will ask about.

## Distributed failure and compensation

### Node failure (availability vs consistency)

| Agent / node | Timeout (s) | Max retries | On exhaust: Consistency (block) or Availability (proceed with gap) | Idempotent on duplicate request? |
| ------------ | ----------- | ----------- | ---------------------------------------------------------------- | -------------------------------- |
| [name] | [n] | [n] | Consistency \| Availability — [why] | yes (natural) \| no — needs idempotency key |

### Saga / compensation (multi-write or multi-produce chain)

| Step | What it produces | Compensating action if later step fails | Who triggers compensation |
| ---- | ---------------- | --------------------------------------- | ------------------------- |
| 1 | […] | […] | [Supervisor / orchestrator node / HITL — name] |
| 2 | […] | […] | [same owner unless stated] |

**Key check:** step N fails after 1..(N-1) ok — undo per step (reverse order), or full restart only? Checkpoint rollback ≠ compensation for external side effects (`ns-langgraph-agents` `error-and-reliability.md`).

## Inter-agent event contracts

Specialists = single request/response, no event bus: one row Event `none`, Pattern `n/a` — **do not** invent events.

| Event name | Emitted by | Payload (fields) | Listeners | Pattern |
| ---------- | ---------- | ---------------- | --------- | ------- |
| `[domain]:[past-tense]` or `none` | [agent] or `n/a` | `{ … }` or `n/a` | [agents] or `n/a` | Sequential \| Parallel \| Supervisor \| Hierarchical \| Group Chat \| Handoff \| Saga \| n/a |

**Rich vs notify:** rich = payload has listener data; notify = signal only, listeners fetch. Mixed: one-line note under table. Skip note when Event is `none`.

## Slide seed (optional)

Paste-ready bullets — no mermaid:

1. **Problem** — [one line]
2. **Recommendation** — framework + topology + one vs many
3. **Why not the alternative** — top rejected option
4. **Risk we accept** — top risk + mitigation
5. **What we cut from MVP** — one line
6. **When we revisit** — architecture change signal
```
