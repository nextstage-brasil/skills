# Agent Platform Report

> Generated by ns-agent-platform skill
> **Handoff:** This document is the single source of truth for compute placement and tenant/platform product. No prior conversation context is required.
> **Prerequisite:** `docs/specs/agent-architecture.md` (ADR). This file does not replace the ADR.
> **Living:** one file for the product. Update in place. Append Changelog. Do not copy into `docs/versions/`.

Write the complete report to `docs/specs/agent-platform.md`. Create `docs/specs/` if missing. Missing: create. Exists: update current-state sections; append Changelog and this Interview Record session. Never blind-replace.

The English headings in this template are the schema for the agent — **the written file** translates every heading, table header, and cell to the human's locked language (Language rule in `SKILL.md`), **except** doctrine ids (Gateway, Orchestrator, Model + Tools/RAG, Approval Gate, Observability) and compute mode names (dedicated, serverless, edge). Those stay English in id columns.

Fill every **applicable** section — no placeholder text.

```markdown
# Agent Platform

> Generated by ns-agent-platform skill
> **Handoff:** Compute topology + tenant/platform product. ADR remains `docs/specs/agent-architecture.md`. Vendor/egress stays in the ADR (`provider-selection.md`).
> **Living:** one file. Update in place. Append Changelog.

## Changelog

| Version | Date | Summary |
| ------- | ---- | ------- |
| `{version_san}` or `adhoc-YYYY-MM-DD` | {ISO date} | {what platform decision changed} |

## Problem Statement

[Restate the request: hosting, tenants, onboarding, LLM gateway product — not framework choice]

## Gate 0 — ADR

- ADR path: `docs/specs/agent-architecture.md`
- Five blocks mapped: yes
- [If missing, this file must not exist — stop and run ns-agent-architecture]

## Compute topology (Gate 1)

One row per ADR five-block. Mixed modes are valid. Do not collapse into one system-wide hosting sentence.

| Block | Mode (dedicated / serverless / edge) | Why | Cold-start (name if serverless; else n/a) | Audit path |
| ----- | -------------------------------------- | --- | ------------------------------------------ | ---------- |
| Gateway | | | | |
| Orchestrator | | | | |
| Model + Tools/RAG | | | | |
| Approval Gate | | | | |
| Observability | | | | central |

**Break-even:** [one worked example from this product's measured concurrency / idle hours] plus explicit “measure your own.” No borrowed course numeric threshold.

**Edge:** [if any edge row: how events reach central Observability]

## Multi-tenant (Gate 2)

| Concern | Shared-once or per-tenant | Why |
| ------- | ------------------------- | --- |
| Runtime image | | |
| Tenant config | | |
| Policy (`AllowlistPolicy` location) | central / copied | |
| Data / checkpointer | | |
| LLM gateway product plane | | |

**Loose Coupling:** [one sentence]
**Clear Interfaces:** [one sentence]
**Policy-Driven Control:** [central vs copied; owner if copied]

Cite `ns-langgraph-agents` `capability-governance.md` `AllowlistPolicy` for runtime shape — do not paste a second allowlist doctrine.

## Onboarding (Gate 3)

| Field | Lock |
| ----- | ---- |
| Approver role | |
| What is reviewed | |
| Skip consequence | |
| Thresholds | inherited (flagged) / reviewed on tenant data |

Inherited rows stay flagged until reviewed. Copy is not a measured lock.

## LLM gateway as product (Gate 4)

| | This product |
| - | ------------ |
| ADR Gateway (API entry) | [one line — not this product] |
| LLM gateway product | [keys, quota, host failover, audit] |
| Prototype → production | [Gate 1 re-run: what changed from local prototype] |

Failover here = hosting/gateway product. Not a second tool-schema copy (`provider-selection.md` stays in ADR).

## Why this design

[Decision record: mixed vs uniform compute, shared vs isolated tenant, gateway product vs route]

## Interview record

| Question | Recommended answer | User reply | Locked decision |
| -------- | ----------------- | ---------- | --------------- |
| | | | |

## Assumptions

| Item | `confirmed` or `assumed` |
| ---- | ------------------------- |
| | |

## Risks

| Risk | Mitigation |
| ----- | ---------- |
| | |

## Next handoff

[`ns-langgraph-agents` only if `graph-spec.md` / agent-api still missing; else infra owner or none — one line]
```
