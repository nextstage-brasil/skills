---
description: mcp-gitlab-usage must never use the ns- skill prefix
alwaysApply: true
---

# Skill naming — `mcp-gitlab-usage`

`mcp-gitlab-usage` is an **intentional exception** to the catalog `ns-` prefix convention.

## Canonical name

- Directory: `skills/mcp-gitlab-usage/`
- Frontmatter `name`: `mcp-gitlab-usage`
- Install / slash: `mcp-gitlab-usage` / `/mcp-gitlab-usage`

## Do not

- Rename to `ns-mcp-gitlab-usage`
- Add `ns-` to this skill's directory, frontmatter, catalog key, or `depends` entries
- Reintroduce `ns-mcp-gitlab-usage` except as a **retired** alias pointing at `mcp-gitlab-usage`

## When editing related files

Keep these in sync with the canonical name (no `ns-`):

- `packages/harness/templates/catalog.json` (`depends` + presets)
- Other skills' `depends` and body references
- `packages/harness/templates/retired-skills.json`: `"ns-mcp-gitlab-usage": "mcp-gitlab-usage"`
- Root `README.md` and `skills/_meta/MIGRATION.md`

See `skills/_meta/MIGRATION.md` — Rename `ns-mcp-gitlab-usage` → `mcp-gitlab-usage` (2026-07-27).
